<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>NextGen.Util</RootNamespace>
    <AssemblyName>NextGen.Util</AssemblyName>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <OutputType>Library</OutputType>
  </PropertyGroup>

  <!--
    Diese Datei existiert in der Original-Estrella-Codebasis nicht (alte
    Compile-Include-Liste hat sie schlicht vergessen); sie ist im aktuellen
    Build bewusst NICHT enthalten, um das bisherige Build-Verhalten exakt
    beizubehalten. Siehe DOCUMENTATION.md, Abschnitt "Bislang ausgeschlossene
    Dateien".
  -->
  <ItemGroup>
    <Compile Remove="SettingsEnum.cs" />
  </ItemGroup>

</Project>
